// Define all routes in the application
const HOME = "/home";
const LOGIN = "/login";
const PROJECTS = "/projects";
const EXPERIENCES = "/experiences";

export const routes = {
  HOME,
  LOGIN,
  PROJECTS,
  EXPERIENCES,
};
